export declare class Throttler {
    private tokens;
    private queue;
    private running;
    private lastTimestamp;
    private refillRate;
    private capacity;
    private delay;
    constructor(config: {
        refillRate: number;
        capacity: number;
        delay: number;
    });
    throttle(cost?: number): Promise<void>;
    private loop;
    private sleep;
}
