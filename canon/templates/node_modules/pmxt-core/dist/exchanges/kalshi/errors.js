"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.kalshiErrorMapper = exports.KalshiErrorMapper = void 0;
const axios_1 = __importDefault(require("axios"));
const error_mapper_1 = require("../../utils/error-mapper");
const errors_1 = require("../../errors");
/**
 * Kalshi-specific error mapper
 *
 * Handles Kalshi API error patterns and message formats.
 */
class KalshiErrorMapper extends error_mapper_1.ErrorMapper {
    constructor() {
        super('Kalshi');
    }
    /**
     * Override to handle Kalshi-specific error patterns
     */
    extractErrorMessage(error) {
        // Handle Kalshi API errors
        if (axios_1.default.isAxiosError(error) && error.response?.data) {
            const data = error.response.data;
            // Kalshi uses nested error.message structure
            if (data.error?.message) {
                const status = error.response.status;
                return `[${status}] ${data.error.message}`;
            }
            if (data.message) {
                return data.message;
            }
            if (data.error && typeof data.error === 'string') {
                return data.error;
            }
        }
        return super.extractErrorMessage(error);
    }
    /**
     * Override to detect Kalshi-specific error patterns
     */
    mapBadRequestError(message, data) {
        const lowerMessage = message.toLowerCase();
        // Kalshi-specific insufficient funds detection
        if (lowerMessage.includes('balance')) {
            return new errors_1.InsufficientFunds(message, this.exchangeName);
        }
        // Fall back to base error mapping
        return super.mapBadRequestError(message, data);
    }
}
exports.KalshiErrorMapper = KalshiErrorMapper;
// Export singleton instance for convenience
exports.kalshiErrorMapper = new KalshiErrorMapper();
