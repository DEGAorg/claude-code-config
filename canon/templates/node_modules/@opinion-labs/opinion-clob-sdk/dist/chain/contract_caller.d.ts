import { type Address, type Hex, type Hash } from 'viem';
import { Signer } from './order_utils/signer.js';
/**
 * Transaction result tuple
 */
export type TransactionResult = {
    txHash: Hash | null;
    safeTxHash: Hex | null;
    returnValue: Hex | null;
};
/**
 * Fee rate settings from FeeManager contract
 */
export interface FeeRateSettings {
    makerMaxFeeRate: number;
    takerMaxFeeRate: number;
    enabled: boolean;
}
/**
 * ContractCaller handles all blockchain interactions
 */
export declare class ContractCaller {
    private safe;
    private signer;
    private multiSigAddr;
    private conditionalTokensAddr;
    private feeManagerAddr;
    private enableTradingCheckInterval;
    private enableTradingLastTime;
    private tokenDecimalsCache;
    constructor(rpcUrl: string, privateKey: Hex, multiSigAddr: Address, conditionalTokensAddr: Address, multiSendAddr: Address, feeManagerAddr: Address, chainId?: number, enableTradingCheckInterval?: number, proxyUrl?: string);
    /**
     * Get signer instance
     */
    getSigner(): Signer;
    /**
     * Get multi-sig address
     */
    getMultiSigAddress(): Address;
    /**
     * Get public client from Safe
     */
    private getPublicClient;
    /**
     * Get ERC20 contract instance
     */
    private getErc20Contract;
    /**
     * Get ConditionalTokens contract instance
     */
    private getConditionalTokensContract;
    /**
     * Get FeeManager contract instance
     */
    private getFeeManagerContract;
    /**
     * Get token decimals with caching
     */
    getTokenDecimals(tokenAddress: Address): Promise<number>;
    /**
     * Check gas balance for signer
     */
    checkGasBalance(estimatedGas?: bigint): Promise<void>;
    /**
     * Get position ID for outcome token
     */
    getPositionId(conditionId: Hex, indexSet: number, collateralToken: Address): Promise<bigint>;
    /**
     * Split collateral into outcome tokens
     */
    split(collateralToken: Address, conditionId: Hex, amount: bigint, partition?: number[]): Promise<TransactionResult>;
    /**
     * Merge outcome tokens back to collateral
     */
    merge(collateralToken: Address, conditionId: Hex, amount: bigint, partition?: number[]): Promise<TransactionResult>;
    /**
     * Redeem outcome tokens after market resolution
     */
    redeem(collateralToken: Address, conditionId: Hex, partition?: number[]): Promise<TransactionResult>;
    /**
     * Enable trading by approving necessary tokens
     */
    enableTrading(supportedQuoteTokens: Map<Address, Address>): Promise<TransactionResult>;
    /**
     * Get fee rate settings from FeeManager contract
     */
    getFeeRateSettings(tokenId: bigint): Promise<FeeRateSettings>;
}
//# sourceMappingURL=contract_caller.d.ts.map