import { type Address } from 'viem';
import { OrderSide } from '../../models/enums.js';
/**
 * Generate a random salt for order uniqueness
 */
export declare function generateSalt(): bigint;
/**
 * Normalize address to checksum format
 */
export declare function normalizeAddress(address: string): Address;
/**
 * Prepend 0x to hex string if not present
 */
export declare function prependZx(value: string): `0x${string}`;
/**
 * Parse price string to numerator and denominator
 * Handles prices with up to 6 decimal places (e.g., "0.123456")
 */
export declare function parsePrice(priceStr: string): {
    numerator: bigint;
    denominator: bigint;
};
/**
 * Calculate order amounts based on price and maker amount
 * Ensures exact price matching using fraction arithmetic
 */
export declare function calculateOrderAmounts(price: number, makerAmount: bigint, side: OrderSide, _decimals?: number): {
    recalculatedMakerAmount: bigint;
    takerAmount: bigint;
};
/**
 * Safely convert human-readable amount to wei units
 */
export declare function safeAmountToWei(amount: number, decimals: number): bigint;
//# sourceMappingURL=utils.d.ts.map