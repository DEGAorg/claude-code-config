import { type Hex } from 'viem';
import { Signer } from './signer.js';
import { type Order, type OrderData, type SignedOrder } from '../../models/order.js';
/**
 * Order builder for creating and signing orders
 */
export declare class OrderBuilder {
    private exchangeAddress;
    private chainId;
    private signer;
    private saltGenerator;
    constructor(exchangeAddress: string, chainId: number, signer: Signer, saltGenerator?: () => bigint);
    /**
     * Get EIP-712 domain
     */
    private getDomain;
    /**
     * Build an order from order data
     */
    buildOrder(data: OrderData): Order;
    /**
     * Create struct hash for signing
     */
    private createStructHash;
    /**
     * Build order signature
     */
    buildOrderSignature(order: Order): Promise<Hex>;
    /**
     * Build and sign an order
     */
    buildSignedOrder(data: OrderData): Promise<SignedOrder>;
    /**
     * Validate order inputs
     */
    private validateInputs;
}
//# sourceMappingURL=order_builder.d.ts.map