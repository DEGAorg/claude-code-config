/**
 * Market/Topic Status
 */
export enum TopicStatus {
  CREATED = 1,
  ACTIVATED = 2,
  RESOLVING = 3,
  RESOLVED = 4,
  FAILED = 5,
  DELETED = 6,
}

/**
 * Market/Topic Type
 */
export enum TopicType {
  BINARY = 0,
  CATEGORICAL = 1,
  ALL = 2,
}

/**
 * Status filter for querying markets
 */
export enum TopicStatusFilter {
  ALL = '',
  ACTIVATED = 'activated',
  RESOLVED = 'resolved',
}

/**
 * Sort type for market listing
 */
export enum TopicSortType {
  BY_TIME_DESC = 1,
  BY_CUTOFF_TIME_ASC = 2,
  BY_VOLUME_DESC = 3,
  BY_VOLUME_ASC = 4,
  BY_VOLUME_24H_DESC = 5,
  BY_VOLUME_24H_ASC = 6,
  BY_VOLUME_7D_DESC = 7,
  BY_VOLUME_7D_ASC = 8,
}

/**
 * Order side
 */
export enum OrderSide {
  BUY = 0,
  SELL = 1,
}

/**
 * Order type
 */
export enum OrderType {
  MARKET_ORDER = 1,
  LIMIT_ORDER = 2,
}

/**
 * Signature type for order signing
 */
export enum SignatureType {
  EOA = 0,
  POLY_GNOSIS_SAFE = 2,
  POLY_PROXY = 1,
}
