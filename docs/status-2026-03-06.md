# Status — 2026-03-06

Post-demo sprint planning. Demo shipped (canon-init, canon-start, terminal-ui,
runner, dashboard). Shifting back to Phase I roadmap work.

---

## 1. Cleanup (Post-Demo Housekeeping)

| # | Task | Notes |
|---|------|-------|
| H1 | Archive demo-prep plan | Move `active/20260303-demo-prep/` to `completed/`. All 9 tasks checked off. |
| H2 | Run `/cleanup` scan | QUALITY.md has no graded areas. First scan populates grades for Core scripts, Canon layer, hooks, commands, docs. |
| H3 | Update CLAUDE.md | "Active Work" section still says "Canon layer now active" generically — update to reflect current Phase I status (demo done, what shipped, what's next). |
| H4 | Audit docs/exec-plans/ | 35 completed plans. Some pre-date the naming convention (`ralph-loop`, `canon-init` without date prefix). Clean up or leave as-is. |
| H5 | Update canon/CLAUDE.md | Verify it reflects what actually shipped in the demo sprint (canon-start, canon-scaffold, terminal-ui, runner). |

## 2. Docs Update

| # | Task | Notes |
|---|------|-------|
| D1 | Verify README.md repo map | Ensure it matches current file structure after demo sprint additions (scripts/, canon/templates/, terminal-ui/). |
| D2 | Review canon agents/skills/commands | Check that all 6 agents, 8 skills, and 6 commands match current implementation and aren't stale from pre-demo assumptions. |
| D3 | Update docs/Dev_Flow.md | Pipeline may need updates to reflect terminal-ui dashboard and runner lifecycle added during demo. |

## 3. Core Init Command

| # | Task | Notes |
|---|------|-------|
| C1 | Create `/core-init` command | New command that bootstraps any repo for Core tools: creates `ralph.yaml`, sets up hooks directory, configures `prek`, writes a minimal CLAUDE.md pointing to the right rules. Mirrors what `/canon-init` does for Canon, but generic — any project, not just prediction markets. |
| C2 | Define Core repo requirements | What a repo needs to work with Ralph Loop + Core tools: `ralph.yaml` at root, `.claude/` hooks directory, exec-plans structure. Document in the command and/or `docs/`. |
| C3 | Wire `/core-init` into `/apply-core` | After `/apply-core` installs global scripts, `/core-init` is available to bootstrap individual repos. |

## 4. Back to Phase I Roadmap

Cross-referenced against `Canon_MVP_Technical_Roadmap.md` (Alternative B) Phase I scope.

### Terminal Wrapper (AI-Controlled Terminal)

| # | Task | Notes |
|---|------|-------|
| T1 | Wrap terminal usage for proper execution | Reviewed existing AI terminal solutions. Current `canon.sh` launches tmux with Claude in the left pane — but the agent uses Bash tool calls, not direct terminal control. Ensure the terminal session runs properly: correct tmux pane targeting, reliable process lifecycle, clean exit handling. |
| T2 | Validate canon-runner.sh lifecycle | Recent commits (`0491def`, `a7821ef`) added `canon-runner.sh` with dashboard lifecycle. Verify edge cases: strategy crash, Claude exit, tmux session cleanup. |
| T3 | Terminal UI state reliability | Ensure `terminal-ui-write.sh` and the Ink dashboard handle concurrent writes, stale state, and long-running sessions without corruption. |

### Canon MCP Server and Phase I Features

| # | Feature | Status | Remaining Work |
|---|---------|--------|----------------|
| R1 | Canon MCP Server (8 tools) | Not started | The critical path. TypeScript MCP server exposing `canon_init`, `canon_register`, `canon_test`, `canon_market`, `canon_position`, `canon_ralph`, `canon_activity`, `canon_help`. Currently operations are done via shell scripts and Claude commands — the MCP server is the agent-agnostic interface. |
| R2 | Canon Arena MVP | Out of Ace scope | Per CLAUDE.md: "Ignore Canon Arena." Arena is separate work. |
| R3 | Simplified Ralph Loop (MCP layer) | Partially done | `ralph-loop.sh` works for Core. The Canon-specific `canon_ralph` MCP tool (Layer 2 of the three-layer split) needs to be built as part of the MCP server. Host hooks (Layer 1) and config (Layer 3) exist. |
| R4 | pmxt integration | Partially done | Demo wired real Polymarket + sportsbook APIs. Needs formalization into the MCP server's `canon_market` and `canon_position` tools via pmxt. |
| R5 | Starter templates (10) | Partially done | `nba-momentum` template exists, plus `client-sportsbook.ts` and `client-polymarket.ts`. 9 more template scaffolds needed per roadmap. |
| R6 | Execution Reliability | Not started | Trade execution test suite, retry/recovery, position reconciliation, monitoring, load testing, circuit breakers. |
| R7 | Cloud Execution Service | Not started | Fly.io Machines provisioning, persistent volumes, crash recovery, Arena event integration. May be Arena-adjacent — check scope. |
| R8 | Developer onboarding | Not started | Quickstart guide: install, scaffold, register in under 15 minutes. |
| R9 | Harness Eng. Patterns | Done | All 7 gaps complete per CLAUDE.md. |
| R10 | Runtime Bridge (minimal) | Not started | Per SAS — application legibility for agents. Skill exists (`app-legibility.md`) but runtime bridge implementation is TBD. |
| R11 | Basic Memory Layer | Not started | Port from Auto-Claude — session persistence across agent restarts. |
| R12 | Workflows (5 YAML) | Partially done | Commands exist (`discover`, `develop`, `register`, `ralph-cycle`, `quick-dev`) as Markdown commands, not YAML workflow definitions per the roadmap spec. |
| R13 | Orchestration config | Not started | `.canon/config.yaml` — context routing, standards injection, agent selection. |

### Suggested Priority Order

1. H1-H5 — Cleanup first (small, fast, clears the decks)
2. C1-C3 — `/core-init` command (enables other repos to use Core)
3. T1-T3 — Terminal wrapper (builds on demo work, informed by video research)
4. R1 — Canon MCP Server (the backbone — everything else plugs into it)
5. R3-R4 — Ralph MCP tool + pmxt wiring (connected to MCP server)
6. R5 — Remaining templates (low complexity, high surface area)
7. R12-R13 — Workflows + orchestration config
8. R8 — Developer onboarding docs
9. R10-R11 — Runtime bridge + memory layer
10. R6-R7 — Execution hardening + cloud service (infrastructure, later)
