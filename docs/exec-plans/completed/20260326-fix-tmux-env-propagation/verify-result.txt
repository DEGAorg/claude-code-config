PASS
All completion criteria verified.

1. GH_SYNC=true visible in engine startup log — orch-engine.sh:279 prints
   "orch-engine: GH_SYNC=${GH_SYNC}". Value propagated from orch-run.sh via
   tmux set-environment (line 371) and inline prefix (line 388).

2. Engine resolves plan path to .orchestrator/plans/ — orch-engine.sh:68-69
   checks GH_SYNC==true and sets PLAN_DIR to worktree .orchestrator/plans/.

3. Reviewer and verifier see GH_SYNC=true — orch-review.sh:172 has inline
   GH_SYNC prefix on reviewer spawn. orch-engine.sh:405 passes GH_SYNC
   inline to orch-verify.sh. Both scripts default GH_SYNC="${GH_SYNC:-false}".

4. shellcheck passes — only SC1091 info notes (sourced files not followed),
   zero errors or warnings on orch-run.sh, orch-engine.sh, orch-review.sh,
   orch-verify.sh.
