DONE:
- Added orch_detect_stale_workers() to scripts/orch-state.sh — checks tmux panes vs running items, handles retry with iteration increment, marks failed after max retries
- Integrated orch_detect_stale_workers() into poll loop in orch-run.sh — called after sync and before promote/spawn, added failed count to poll log
- Hardened iteration increment and max-retry failure logic with jq defaults (// 0 for iteration, // 3 for maxIterations) to prevent bash arithmetic failures on missing fields
- Test: kill a worker pane mid-run, verify item gets re-spawned — tests/test-orch-stale-detection.sh, 3 tests, 6 assertions, all pass

REMAINING:
- Test: kill a worker 3 times, verify item marked "failed" after max iterations

BLOCKERS:
- none
