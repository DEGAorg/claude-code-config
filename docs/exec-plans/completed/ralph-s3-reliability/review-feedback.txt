CRITERION: bash scripts/ralph-check.sh exits 0
FINDING: ralph-check.sh exits 1. The task-claimed criterion fails:
  ✗ task-claimed: files changed but current task not marked complete
  RESULT: 5/6 criteria passing. Keep working.
The .ralph-state.json file (docs/exec-plans/active/ralph-s3-reliability/.ralph-state.json)
has "claimed_complete": false while git diff HEAD shows uncommitted changes. The worker
claims it "set claimed_complete=true in state file" but the file on disk reads false.
ACTION: Update .ralph-state.json so that current_task.claimed_complete is true:
  jq '.current_task.claimed_complete = true' \
    docs/exec-plans/active/ralph-s3-reliability/.ralph-state.json > /tmp/state.tmp \
    && mv /tmp/state.tmp docs/exec-plans/active/ralph-s3-reliability/.ralph-state.json
Then verify: bash scripts/ralph-check.sh must exit 0 with all 6/6 criteria passing.
