CRITERION: All GitHub raw URLs in canon-init resolve (premar-demo branch)
FINDING: All 9 modified files are uncommitted and unpushed. origin/premar-demo still has old content with canon_ MCP tool references in agents (dev.md, qa.md), skills (backtesting.md, ralph-loop.md, risk-management.md), and commands (develop.md, ralph-cycle.md, discover.md, canon-init.md). The URLs resolve (HTTP 200) but serve stale content.
ACTION: Commit all 9 modified files and push to origin/premar-demo. This is a feature branch — pushing is safe.

CRITERION: /develop runs without referencing nonexistent MCP tools
FINDING: Local canon/commands/develop.md is correctly fixed (zero canon_ refs). But GitHub serves the old version which still has canon_init, canon_test, and canon_ralph references. When /canon-init fetches develop.md from GitHub into a clean project, the fetched file would have MCP refs.
ACTION: Same fix — commit and push makes the correct version available at the GitHub URL.

CRITERION: /ralph-cycle runs without referencing nonexistent MCP tools
FINDING: Same as /develop — local file is correct, GitHub serves stale version with canon_ralph and canon_test refs.
ACTION: Same fix — commit and push.

CRITERION: End-to-end: clean dir → /canon-init → Canon context fully loaded
FINDING: Cannot pass until GitHub serves updated content. A real end-to-end test after push is needed to confirm all 19 files land with correct (MCP-free) content.
ACTION: After committing and pushing, re-run the end-to-end verification in a fresh temp directory to confirm fetched files match local versions.
