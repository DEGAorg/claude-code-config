PASS
All completion criteria verified.

1. core-init command always produces a `github:` block — VERIFIED
   Step 4 has 3-phase detection with fallback, step 5 has bold mandatory instruction,
   step 6 has validation gate with auto-repair.

2. Missing `gh` CLI results in `sync: false` with explanatory comment — VERIFIED
   Fallback template: `github:\n  # gh CLI not available — install gh and run /core-init again to enable sync\n  sync: false`

3. Validation gate catches missing block and reports error — VERIFIED
   Step 6 uses `grep -q '^github:' dega-core.yaml`, prints ERROR, appends fallback,
   re-verifies with FATAL on second failure.

4. shellcheck passes on any new scripts — VERIFIED
   `shellcheck tests/test-core-init-no-gh.sh scripts/orch-state.sh` — clean (exit 0).
