--- item: Replace sed with bash expansion for worker prompt substitution (lines 228-231) ---
Replaced the sed command on lines 228-231 of scripts/ralph-loop.sh with bash parameter
expansion. The worker prompt template is now read via `cat "${WORKER_PROMPT}"` and then
two `${WORKER_CONTEXT//\{PLACEHOLDER\}/${VALUE}}` expansions handle {TASK_DIR} and
{STATE_FILE}. This matches the proven pattern from orch-review.sh lines 95-99. The
surrounding handoff-append logic (lines 231-241) was untouched.
---
--- item: Replace sed with bash expansion for reviewer prompt substitution (lines 368-373) ---
Replaced the two sed commands on lines 367-372 of scripts/ralph-loop.sh with bash
parameter expansion. The reviewer prompt template is now read via `cat "${ITEM_REVIEWER_PROMPT}"`
and four `${ITEM_PROMPT//\{PLACEHOLDER\}/${VALUE}}` expansions handle {ITEM_TEXT},
{ITEM_NUM}, {REVIEW_DIR}, and {ITEM_HANDOFF}. The second sed (piped through printf) that
was crashing on multiline HANDOFF content is eliminated. No other lines changed.
---
--- item: Test: run ralph-loop.sh on a plan where context-handoff.txt has multiline content ---
Verified all three completion criteria: (1) no sed template substitution remains — only
structural sed on line 267 for completion-criteria extraction, (2) shellcheck and shfmt
both pass clean, (3) bash parameter expansion tested with multiline handoff containing
special characters (&, \, /, pipe delimiters, quotes) — all 12 assertions passed across
both worker and reviewer prompt expansion paths.
---
