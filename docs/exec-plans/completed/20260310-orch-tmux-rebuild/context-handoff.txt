--- item: Add `poll_interval_seconds` to `ralph.yaml` with default 30 ---
Added `poll_interval_seconds: 30` as a top-level key in `ralph.yaml` (line 8),
placed after `check_command` and before the worker/reviewer prompt templates.
Only file changed: `ralph.yaml`. The orchestrator (`orch-run.sh`) should read
this value to configure its polling interval instead of hardcoding 30 seconds.
No schema validation exists for ralph.yaml, so consumers must handle the field
being absent (fallback to 30) for backward compatibility with other repos.
---

--- item: Add `orch_promote_ready_items` to `scripts/orch-state.sh`: for each queued item, if all deps are done, set status to ready ---
Added `orch_promote_ready_items` function to `scripts/orch-state.sh` (lines 97-140).
It reads state.json, finds all items with status "queued", checks if their deps
array is empty or all dep items have status "done", and promotes them to "ready".
Uses `orch_write_state` for atomic writes and logs the count of promoted items.
The jq logic mirrors the inline promotion in `orch-run.sh` lines 82-92 — the
next worker should refactor `orch-run.sh` to call this function instead of
duplicating the logic inline. shellcheck and shfmt both pass clean.
---

--- item: Rewrite `scripts/orch-run.sh` execution engine: create tmux session, launch dashboard pane, implement wave loop (spawn workers in panes, poll done-files, promote items, advance waves) ---
Replaced the TODO stub (old lines 170-181) with a full tmux execution engine.
Key changes: (1) reads `poll_interval_seconds` from ralph.yaml with fallback to 30,
(2) creates tmux session `orch-<slug>` with a dashboard window running `watch jq`
on state.json, (3) `build_worker_prompt()` constructs per-item prompts from
`agents/orch-worker.md` template with dep summaries capped at 5, (4) `spawn_worker()`
writes prompt to temp file and launches `claude -p` in a named tmux window
(`worker-<id>`), (5) wave loop polls every N seconds using `orch_sync_done_files`
and `orch_promote_ready_items` from orch-state.sh, spawns ready items up to
max-workers concurrency, (6) after all items complete, runs `orch-review.sh` and
handles SHIP (cleanup + sound) or REVISE (exec self for rework loop). Also removed
the duplicate inline jq promotion logic from `init_state` in favor of calling
`orch_promote_ready_items`. shellcheck and shfmt both pass clean.
---

--- item: Add rework feedback section to `agents/orch-worker.md` for review-iteration context (deps: 3) ---
Added a "Rework iterations" section to `agents/orch-worker.md` (between
"Write the done-file" and "Rules") explaining how workers should handle
REVISE feedback: read all points, fix them, re-run linters, overwrite the
done-file. Also updated `build_worker_prompt()` in `scripts/orch-run.sh`
to inject per-item review feedback from `.orchestrator/reviews/<slug>/item-<id>-review.txt`
when the file exists. Both shellcheck and shfmt pass clean.
---

--- item: Smoke test: reset `20260309-orch-smoke-test` state and run end-to-end with new tmux engine (deps: 3, 4) ---
Reset smoke test state (unchecked plan.md, cleared state.json and done-files),
then ran `orch-run.sh 20260309-orch-smoke-test --max-workers 4` end-to-end.
Found and fixed two bugs: (1) dashboard pane used `watch` (not installed on
macOS) — replaced with a portable `while/sleep/clear` loop in orch-run.sh
line 183, (2) macOS `mktemp` doesn't support suffixes after XXXXXX — added
`mv` rename after mktemp in `spawn_worker()` at lines 266-268. All 8 items
completed in correct wave order (1 -> 2,3,4 -> 5,6 -> 7 -> 8), all 8
per-item reviews PASS, final result SHIP. Files changed: `scripts/orch-run.sh`.
---

--- item: Update `commands/apply-core.md` install manifest to reflect current orchestrator scripts (deps: 5) ---
Verified that all orchestrator files (orch-run.sh, orch-state.sh,
orch-parse-items.sh, orch-review.sh, orch-worker.md, and 4 Ink components)
were already listed in the Source, Inventory, and Install sections. Added
`poll_interval_seconds` to the Orchestrator component description (line 169)
and to the ralph.yaml customization warning (line 274). File changed:
`commands/apply-core.md`. This is the final item — all progress log items done.
---
