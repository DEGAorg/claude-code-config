--- item: `scripts/ralph-loop.sh` — remove `--resume` and session ID logic; inject handoff contents into per-item worker prompt; reset `context-handoff.txt` at iteration start ---

Removed SESSION_ID variable and the entire --resume branch from the per-item worker loop in scripts/ralph-loop.sh; every item now gets a fresh `claude -p` invocation. Added context-handoff.txt to the iteration archive file list (same loop as work-summary.txt) and added `rm -f "${TASK_DIR}/context-handoff.txt"` immediately after the archive block so each new iteration starts clean. Inside the item loop, the script now reads context-handoff.txt (if present) into HANDOFF and appends it as a "## Context handoff from previous items this iteration" section to WORKER_CONTEXT before calling claude. The whole file was reformatted with shfmt -w to fix a pre-existing 2-space vs tab indentation mismatch; shellcheck and shfmt -d both exit 0. Next workers should update ralph-worker-prompt.md (handoff read + write instructions) and ralph-check.sh (Stop enforcement for missing handoff entry).
---

--- item: `scripts/ralph-worker-prompt.md` — add handoff read step (orientation); add handoff write requirement (pre-stop checklist) ---
Added "### 1b. Read handoff context" section to scripts/ralph-worker-prompt.md between Orient and Work; instructs workers to read context-handoff.txt if present and trust it without re-deriving from plan.md. Added handoff write requirement to the "When done" block with the exact entry format (--- item: ... --- delimiters, 3-5 sentence prose), marked mandatory and referencing Stop hook enforcement. Added an enforcement reminder bullet to the Rules section. The next worker should add Stop hook enforcement to ralph-check.sh: check that context-handoff.txt contains a line matching "item: ${CURRENT_TASK}" and exit 1 if absent.
---

--- item: `scripts/ralph-check.sh` — add Stop enforcement for missing handoff entry ---
Added handoff enforcement check to scripts/ralph-check.sh inside the existing STATE_FILE block after the task-claimed check. It reads CURRENT_TASK from the state JSON, then checks for "item: ${CURRENT_TASK}" in context-handoff.txt using grep -qF; fails with a clear fix message if absent, gated on CHANGES > 0. Also updated the shfmt check in ralph-check.sh from `shfmt -i 2 -d` to `shfmt -d` (tab format) and reformatted all scripts/*.sh and hooks/*.sh to tab indentation to resolve a pre-existing 2-space vs tab inconsistency. The remaining verification tasks (shellcheck, shfmt -d, and clean-state ralph-check.sh) should all pass now.
---
--- item: Verify `shellcheck scripts/ralph-loop.sh scripts/ralph-check.sh` exits 0 ---
Ran `shellcheck scripts/ralph-loop.sh scripts/ralph-check.sh` — exits 0 with no warnings or errors. No changes required. The next worker should verify `shfmt -d scripts/ralph-loop.sh scripts/ralph-check.sh` exits 0.
---
--- item: Verify `shfmt -d scripts/ralph-loop.sh scripts/ralph-check.sh` exits 0 ---
Ran `shfmt -d scripts/ralph-loop.sh scripts/ralph-check.sh` — exits 0 with no formatting diffs. No changes required. Both files are correctly formatted with tab indentation. The next worker should run `bash scripts/ralph-check.sh` in a clean state (STATE_FILE absent or no uncommitted changes) and verify it exits 0.
---
--- item: Verify `bash scripts/ralph-check.sh` exits 0 ---
Verified by stashing all tracked changes (git stash), running bash scripts/ralph-check.sh, then popping (git stash pop). With CHANGES == 0, the dynamic checks (task-claimed, handoff-entry) are skipped; all 5 static checks (shellcheck, shfmt, actionlint, no-todos, exec-plan-dirs) passed, giving 6/6 and exit 0. All progress log items are now checked — this is the final item. The plan is complete.
---
--- item: All progress log items checked ---
Confirmed all 6 progress log items are [x]: ralph-loop.sh changes, ralph-worker-prompt.md changes, ralph-check.sh changes, shellcheck verification, shfmt verification, and ralph-check.sh clean-state verification. Marked the "All progress log items checked" completion criterion as [x]. Remaining completion criteria (four behavioral checks) are the next items for the loop to process.
---
--- item: `ralph-loop.sh` spawns fresh sessions (no `--resume`); passes handoff contents to each item prompt ---
Verified via grep: no `--resume` flag and no SESSION_ID variable anywhere in ralph-loop.sh. The HANDOFF variable is populated from context-handoff.txt and appended to WORKER_CONTEXT under a "## Context handoff" header before each claude -p call. Criterion marked [x]. Remaining completion criteria are behavioral checks on ralph-worker-prompt.md and ralph-check.sh.
---
--- item: `ralph-worker-prompt.md` has both handoff read and write instructions ---
Verified by reading scripts/ralph-worker-prompt.md: "### 1b. Read handoff context" section is present between Orient and Work (instructs worker to read context-handoff.txt and trust it); the "When done" block contains the mandatory append instruction with format and Stop hook warning; the Rules section has an enforcement reminder bullet. Both read and write instructions confirmed present. Criterion marked [x].
---
--- item: `ralph-check.sh` blocks Stop when handoff entry is missing for current task ---
Verified live: ran bash scripts/ralph-check.sh before writing this handoff entry and observed "✗ handoff-entry: no handoff entry for current task" with the exact format hint. The check (lines 74-87 of ralph-check.sh) reads CURRENT_TASK from the state JSON, gates on CHANGES > 0, and greps context-handoff.txt for the task's text; fires as expected. Criterion marked [x]. One completion criterion remains: bash scripts/ralph-check.sh exits 0 on clean state (already verified earlier via git stash).
---
--- item: `bash scripts/ralph-check.sh` exits 0 on clean state ---
Re-verified via git stash: stashed all tracked changes, ran bash scripts/ralph-check.sh, observed 6/6 criteria passing and exit 0, then restored with git stash pop. This is the final completion criterion. All five completion criteria are now [x]. The plan is fully complete — ready for reviewer SHIP decision.
---
