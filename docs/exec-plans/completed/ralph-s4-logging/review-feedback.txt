CRITERION: All progress log items checked
FINDING: The progress log item "Verify `shfmt -d hooks/session-start-logging.sh
hooks/structured-log.sh` exits 0" is marked [x] but the command exits 1 when
run. The files use 2-space indentation (matching the bash standard: shfmt -i 2),
while bare `shfmt -d` (no -i flag) defaults to tab indentation and therefore
reports differences. The worker marked the item done without the command actually
passing.

Observed output:
  diff hooks/session-start-logging.sh.orig hooks/session-start-logging.sh
  @@ -21,4 +21,4 @@
  -  "$(date -u +%Y-%m-%dT%H:%M:%SZ)" >"$CONFIG"
  +	"$(date -u +%Y-%m-%dT%H:%M:%SZ)" >"$CONFIG"
  shfmt exit: 1

Note: `shfmt -i 2 -d hooks/session-start-logging.sh hooks/structured-log.sh`
exits 0. The scripts are correctly formatted per the bash standard. Only the
command text in the progress log item is wrong.

ACTION: Update the progress log item in plan.md from:
  - [x] Verify `shfmt -d hooks/session-start-logging.sh hooks/structured-log.sh` exits 0
to:
  - [x] Verify `shfmt -i 2 -d hooks/session-start-logging.sh hooks/structured-log.sh` exits 0

Then confirm `shfmt -i 2 -d hooks/session-start-logging.sh hooks/structured-log.sh`
still exits 0 (it does now), and verify `bash scripts/ralph-check.sh` still exits 0.
