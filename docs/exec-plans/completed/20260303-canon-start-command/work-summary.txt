DONE:
- Created canon/commands/canon-start.md with phase detection logic (init -> scaffold -> strategy -> develop -> run)
- Rewrote Step 1 (tmux session launch) with complete instructions: tmux+script prerequisite checks, existing session detection, state file creation, detached session creation with dashboard pane, and user guidance for attaching
- Verified all 12 state file write points across every phase transition against terminal-ui-write.sh API; fixed initial state creation to use terminal-ui-write.sh with complete JSON fallback
- Inlined canon-init procedure into step 3 with full URLs, file templates, and config content (sub-sections 3a-3i); updated step 4 scaffold reference to point to step 3 templates; added RiskInterface enforcement warning to step 6a
- Added existence guards to all 9 bare terminal-ui-write.sh calls in steps 3-7; added "State write convention" note between steps 1 and 2; updated Graceful degradation reference section to reflect inline guards
- Verified all file paths, GitHub URLs, script paths, API usage, and tool names; cross-referenced 19 GitHub URLs against canon-init.md (all match); fixed AGENTS.md template to include missing "Key Workflows" and "Domain Knowledge (Skills)" sections
- Verified completion criterion: `canon/commands/canon-start.md` exists with all phase logic (732 lines, all 5 phases present)
- Verified completion criterion: command detects fresh project (no `.canon/`) and handles init — step 2 first condition routes to step 3 with complete inline init procedure
- Verified completion criterion: command detects existing project and skips to correct phase — step 2 evaluates 4 conditions for existing projects (scaffold, strategy, develop, run) with proper inter-step flow
- Verified completion criterion: state file writes at each phase transition — all 12 guarded terminal-ui-write.sh calls present covering every phase entry and exit
- Verified completion criterion: tmux session launch is included at entry — step 1 has full tmux launch with prerequisite checks, graceful degradation, session detection, state file init, detached session creation, renderer priority, and user attach instructions
- Verified completion criterion: graceful degradation documented — step 1 prints one note for missing tmux/scripts, all 12 terminal-ui-write.sh calls guarded, dedicated degradation table in lines 705-720

REMAINING:
- none — all progress log items and completion criteria are checked

BLOCKERS:
- none
