DONE:
- Created skills/app-legibility.md with all four patterns:
    1. Log file redirection (stdout/stderr → logs/server.log)
    2. Per-worktree port and database isolation (derived from worktree name)
    3. Health endpoint (GET /health → JSON with status, uptime, timestamp)
    4. Crash surfacing (uncaughtException/unhandledRejection → logs/crashes.log)
  Each pattern includes problem statement, solution, code examples for both
  Node/TypeScript and Python, and agent usage instructions. File ends with a
  four-row checklist agents can use to verify a service is legible.
- Updated CLAUDE.md: gap 7 row changed from "**To do** | App-legibility skill"
  to "**Done** | `skills/app-legibility.md`". All seven harness gaps now show Done.
- Updated commands/apply-core.md: added skills/app-legibility.md to the file
  list (Source section) and updated the Skills component description to include
  app-legibility.
- Marked all progress log and completion criteria checkboxes [x] in plan.md.

REMAINING:
- none

BLOCKERS:
- none
