PASS
All completion criteria verified.

1. After SHIP, tmux session auto-closes within ~15 seconds
   - orch-engine.sh:567-569: 10s sleep then tmux kill-session. Within 15s budget.

2. After FAILED, tmux session auto-closes within ~10 seconds
   - orch-engine.sh:601-604: 5s sleep then tmux kill-session. Within 10s budget.

3. state.json has "status": "completed" before session closes
   - orch-engine.sh:499-503: writes status before validation/summary/sleep/kill sequence.

4. Dashboard renders SHIP screen before session closes
   - State written at line 503, dashboard polls via chokidar, 10s delay at line 568 gives ample time.

5. shellcheck passes on modified files
   - shellcheck -S warning scripts/orch-engine.sh scripts/planner-loop.sh: clean (0 warnings/errors).
   - SC1091 info notes (not following sourced files) are expected and not actionable.
